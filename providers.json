{
  "_comment": "Provider registry. Edited from /admin, committed to this public repo like any method change. The web app never executes `command`; only the local Python pipeline does. See lib/providers.ts for the security boundary. Extractor order is priority: the first enabled extractor wins, and an enabled API entry beats every CLI below it. The run record stores which one actually ran.",
  "providers": [
    {
      "id": "chatgpt",
      "label": "ChatGPT",
      "kind": "api",
      "role": "engine",
      "enabled": true,
      "env": "OPENAI_API_KEY"
    },
    {
      "id": "claude",
      "label": "Claude",
      "kind": "api",
      "role": "engine",
      "enabled": true,
      "env": "ANTHROPIC_API_KEY"
    },
    {
      "id": "gemini",
      "label": "Gemini",
      "kind": "api",
      "role": "engine",
      "enabled": false,
      "env": "GEMINI_API_KEY",
      "note": "Adapter works and is tested; the models do not. Measured 2026-08-26: gemini-3.5-flash failed 31% of a full week with 503 UNAVAILABLE (and 5 of 8 sequentially, so it is Google's overload, not our concurrency). gemini-3.6-flash answers reliably but chooses to search on roughly a fifth of calls, which measures what the model remembers rather than what it retrieves. Grounding cannot be forced: google_search_retrieval returns 400 and tool_config ANY times out. The whole 2.5 line is 404 for new keys. Re-enable when a stable, reliably-grounded model exists, and bump method_version in the same change."
    },
    {
      "id": "perplexity",
      "label": "Perplexity",
      "kind": "api",
      "role": "engine",
      "enabled": true,
      "env": "PERPLEXITY_API_KEY"
    },
    {
      "id": "claude-api-extract",
      "label": "Claude (API)",
      "kind": "api",
      "role": "extractor",
      "enabled": true,
      "env": "ANTHROPIC_API_KEY",
      "note": "Hosted batch extractor. Earlier comparison with 150 archived answers measured agreement, not a validated noise floor or extraction accuracy. Declared conflict: it reads answers about its own products."
    },
    {
      "id": "claude-cli",
      "label": "Claude Code (local CLI)",
      "kind": "cli",
      "role": "extractor",
      "enabled": true,
      "command": "claude",
      "args": [
        "-p",
        "--strict-mcp-config"
      ],
      "note": "CLI extraction is disabled by the resolver until filesystem and network isolation are qualified. This entry does not provide a fallback when the hosted API is unavailable."
    },
    {
      "id": "codex-cli",
      "label": "Codex (local CLI)",
      "kind": "cli",
      "role": "extractor",
      "enabled": true,
      "command": "codex",
      "args": [
        "exec",
        "--skip-git-repo-check",
        "--ignore-user-config",
        "--ignore-rules",
        "--sandbox",
        "read-only",
        "-"
      ],
      "note": "CLI extraction is disabled by the resolver until filesystem and network isolation are qualified. Local engine operation is configured separately."
    },
    {
      "id": "claude-code",
      "label": "Claude Code (local)",
      "kind": "cli",
      "role": "engine",
      "enabled": true,
      "command": "claude",
      "args": [
        "-p",
        "--strict-mcp-config"
      ],
      "note": "Own chart row, not a substitute for the hosted claude engine: this is a coding agent with a different system prompt, and it volunteers that it is made by Anthropic. Enabled, which means the weekly run must happen on this machine: see scripts/weekly-run.cmd."
    },
    {
      "id": "codex",
      "label": "Codex (local)",
      "kind": "cli",
      "role": "engine",
      "enabled": true,
      "command": "codex",
      "args": [
        "exec",
        "--skip-git-repo-check",
        "--ignore-user-config",
        "--ignore-rules",
        "--sandbox",
        "read-only",
        "-"
      ],
      "note": "Own chart row, not a substitute for the hosted chatgpt engine. In testing it ranked Claude Code above OpenAI's own Codex. Enabled for the same reason as claude-code."
    }
  ]
}
